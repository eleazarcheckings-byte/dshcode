/** Predetermined runnable page for the keyless retention test, not generated design evidence. */
export const FIRST_ARTIFACT = '<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Saved first version</title><main><h1>Saved first version</h1><button type="button" onclick="document.querySelector(\'#status\').textContent=\'Selected\'">Select this item</button><p id="status" role="status">Ready</p></main></html>\n'

/** Text a truncated refinement must never write over the saved page. */
export const UNFINISHED_REFINEMENT = 'This incomplete refinement must not replace the working page.'
