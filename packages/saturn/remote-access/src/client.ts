/** Browser-safe remote-access shapes; the runtime transport is the generated Remote. */
export type {
  RemoteDeviceRecord,
  RemoteEvent,
  RemoteEventType,
  RemoteIssue,
  RemoteJournalEntry,
  RemoteMode,
  RemotePairingPayload,
  RemoteState,
  RemoteStatus,
} from './types.ts'
